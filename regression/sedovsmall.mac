#!/bin/bash

mkdir -p ${HOME}/local/regression/pennant/sedovsmall  #${SLURM_JOB_ID}
cd ${HOME}/local/regression/pennant/sedovsmall  #${SLURM_JOB_ID}
cp ${HOME}/github/PENNANT//pennant . 
cp ${HOME}/github/PENNANT/test/sedovsmall/sedovsmall.pnt  . 
./pennant sedovsmall.pnt
