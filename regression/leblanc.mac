#!/bin/bash

mkdir -p ${HOME}/local/regression/pennant/leblanc  #${SLURM_JOB_ID}
cd ${HOME}/local/regression/pennant/leblanc  #${SLURM_JOB_ID}
cp ${HOME}/github/PENNANT//pennant . 
cp ${HOME}/github/PENNANT/test/leblanc/leblanc.pnt  . 
./pennant leblanc.pnt
