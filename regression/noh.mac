#!/bin/bash

mkdir -p ${HOME}/local/regression/pennant/noh  #${SLURM_JOB_ID}
cd ${HOME}/local/regression/pennant/noh  #${SLURM_JOB_ID}
cp ${HOME}/github/PENNANT//pennant . 
cp ${HOME}/github/PENNANT/test/noh/noh.pnt  . 
./pennant noh.pnt
